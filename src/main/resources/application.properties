spring.application.name=ProductService
#It acts as both env variables & default port (Here default port is a fallback mechanism)
#server.port=${SERVER_PORT:5000}

server.port=8081

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/ProductService
spring.datasource.username=ProductServiceUser
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql: true

logging.level.org.springframework.web.*=TRACE

# AWS Configs (added in environment variables)
#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}

